require.config({
    baseUrl: 'js'
});

require(['routes']);

// require(['tabs']);
require(['home']);

require(['connectionDB']);
require(['main']);

require(['signUp']);
// require(['signIn']);
require(['map']);


require(['jsonParse']);
